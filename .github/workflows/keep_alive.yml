name: Keep App Awake

on:
  schedule:
    - cron: '0 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the screener
        run: |
          curl -I https://projectbot666.streamlit.app
          
